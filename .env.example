# Follow Up Boss CRM — used by /api/lead to forward form submissions.
# Get this from Sunny's Follow Up Boss account (Admin > API).
# See the TODO comments in app/api/lead/route.ts for integration details.
FOLLOW_UP_BOSS_API_KEY=

# Google Maps Embed API key — enables the map on listing detail pages.
# https://developers.google.com/maps/documentation/embed/get-api-key
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
